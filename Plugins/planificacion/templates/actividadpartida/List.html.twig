{% extends "Master/ListController.html.twig" %}

{% block bodyTop %}
<div class="container-fluid mb-3">
    <div class="row">
        <div class="col-sm-6">
            <h1><i class="fas fa-link"></i> Actividades y Partidas</h1>
        </div>
        <div class="col-sm-6 text-right">
            <a href="{{ fsc.url() }}?action=new" class="btn btn-success">
                <i class="fas fa-plus"></i> Relacionar Actividad y Partida
            </a>
        </div>
    </div>
</div>
{% endblock %}

{% block body %}
<div class="table-responsive">
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Actividad</th>
                <th>Partida</th>
                <th>Ítem PAC</th>
                <th>Descripción</th>
                <th class="text-right">Acciones</th>
            </tr>
        </thead>
        <tbody>
            {% for relacion in fsc.relaciones %}
            <tr>
                <td>{{ fsc.getActividadNombre(relacion.actividad_id) }}</td>
                <td>{{ fsc.getPartidaNombre(relacion.partida_id) }}</td>
                <td>{{ fsc.getPacItemNombre(relacion.pac_item_id) }}</td>
                <td>{{ relacion.descripcion }}</td>
                <td class="text-right">
                    <a href="{{ fsc.url() }}?action=delete&actividad_id={{ relacion.actividad_id }}&partida_id={{ relacion.partida_id }}" class="btn btn-danger btn-sm" onclick="return confirm('¿Está seguro?')">
                        <i class="fas fa-trash-alt"></i> Eliminar
                    </a>
                </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="5" class="text-center">No hay relaciones registradas</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}